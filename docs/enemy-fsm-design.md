# Enemy FSM 设计草稿

状态：设计讨论稿，待最终审阅。以 `3028748` 的代码为现状依据；下面的 FSM 尚未实现或运行验证。

## 当前行为与改动意图

当前 `EnemyController` 持续追踪玩家；`ContactDamage.OnCollisionEnter2D` 在碰撞进入时扣血；`EnemyHealth` 管理生命并在归零时销毁；`WaveSpawner` 负责配置检查、生成、目标注入、波次和玩家死亡后的停机。

新设计增加出生点附近的慢速随机巡逻，并将碰撞伤害替换为受距离和冷却限制的主动攻击。这两项是有意改变玩法，不是行为等价重构；配置读取、目标注入、玩家受伤/UI、敌人死亡、波次和重开仍需回归。

## 状态与转移

以下是本人手绘图与讨论补充的文字整理，不另制作重复转移表。`d` 表示敌人与玩家距离，`enterRange < leaveRange`；具体数值尚未决定。

- **Idle**：在出生点附近慢速随机巡逻；不负责主动攻击。
- **Chase**：朝玩家快速移动，复用现有追踪计算；满足攻击条件后才进入 Attack。
- **Attack**：允许移动，主动调用玩家扣血，一次攻击结束后返回 Chase；不再依靠碰撞扣血。首次攻击也要等待冷却，不能生成后立即跳过间隔。
- **Dead**：停止行动并取消尚未结算的攻击；无返回存活状态的边。已结算伤害不回滚。

已确认的边：

- Idle → Chase：`d < enterRange`。
- Chase → Idle：`d >= leaveRange`。
- Chase → Attack：`d < attackRange`，且距上次攻击的时间 `>= attackInterval`。
- Attack → Chase：本次攻击执行完毕。
- Idle / Chase / Attack → Dead：`health <= 0`，死亡优先于普通行为和转移。

等于触发距离时，Idle 不进入追击；等于脱离距离时，Chase 退出追击。两个追击阈值之间，Idle/Chase 保持原状态，不因追击距离条件相互切换；Chase 的攻击判定仍独立进行。未列出的敌人状态边视为非法边。

初始化不增加一个长期运行状态：先检查依赖，再按距离选择初态。原图只有严格大小比较；初始等号分支及初始距离落在双阈值区间时的选择，待本人最终明确，不能据草稿认定已经覆盖。

## 全局规则与组件职责

- **缺少初始目标就快速失败**：Spawner 启动时检查所用 Prefab、必需组件与场景玩家引用；实例的 `Initialize` 检查注入目标，失败报错、不启动 FSM。Prefab 模板允许 target 为空，不能把模板尚未注入目标误判为故障。已初始化后目标意外失效的处理仍待确定。
- **玩家死亡全部停机**：停止生成，现存敌人停止移动和攻击，取消未结算攻击；这不是敌人死亡，不进入 Dead，也不退回巡逻。
- **生命与 FSM 分工**：`EnemyHealth` 保留血量、归零判断和最终销毁职责；拟在销毁前通知 `EnemyController`，由统一切换入口进入 Dead 并终止行为。具体通知接口尚未实现，不依赖对象销毁后下一帧再轮询死亡。
- **停止行为必须真实生效**：当前 Spawner 只禁用 `EnemyController`；若未来存在独立组件、协程或延迟回调，必须确保这些攻击不会在停机后继续执行。

## 实现方案决定

已选择 **enum + switch**，转移判断和统一切换入口先留在 `EnemyController`，明确调用 exit / entry action。枚举定义可单独放 `EnemyState.cs`；当前状态、冷却等运行字段属于每个敌人实例，不做共享静态数据。

- **enum + switch**：当前成本最低，复用组件生命周期、便于追踪调用顺序；状态增加后容易形成大类。独立测试是否方便取决于条件判断是否与 Unity 操作混在一起。
- **普通 C# state class**：有利于拆分复杂行为和复用；需要明确上下文依赖，由控制器主动调用更新与进入/退出方法。依赖设计好才利于独立测试，并非拆文件就自动解耦。待状态代码明显膨胀或真实复用需求出现时再考虑。
- **ScriptableObject state asset**：方便通过资产组合与复用行为，但增加资产、引用及运行上下文管理成本；共享资产不能混放各敌人的计时器等状态，资产生命周期也不等于敌人的状态切换。当前不需要这层配置能力。

## BLU-16 最小改动草案

1. `EnemyState.cs`：仅增加枚举定义，不单独创建 FSM 框架。
2. `EnemyController.cs`：增加实例状态、统一切换入口、条件判断、各状态行为和停机处理；保留目标注入、配置读取、Rigidbody2D 引用及可复用的追踪计算。预计需要检测/脱离/攻击距离、攻击间隔与伤害、上次攻击时间、出生点及巡逻参数；字段命名、参数存放位置、初值与最小只读观察入口尚未最终确定。
3. 相关敌人 Prefab：停用旧 `ContactDamage`，保留碰撞器。无需为保留旧文件而重写碰撞逻辑；伤害数值迁移到主动攻击路径时需核对现有序列化值。
4. `EnemyHealth.cs`：接入销毁前的死亡通知，保留血量处理与唯一销毁职责，避免重复死亡副作用。
5. `WaveSpawner.cs`：暂按不改规划，保留校验、生成和目标注入；以禁用控制器能完整停止新行为为前提，若验证不成立，再局部调整停机接口。

今天只写草稿，不实施上述修改；BLU-17 的日志和 Debug 显示也不提前交付。

## 待明确的细节

- 攻击间隔从哪个时刻重新计时，以及 Attack 中移动逻辑如何复用。当前不因未来可能有攻击前摇而主动加入动画或延迟机制。
- 巡逻参数按本人决定暂留空；检测方式、条件求值时机和每个状态的具体 entry / exit action 尚未最终确定。
- 初始状态边界、运行中目标失效规则（见上文）。

## 验证清单（尚未执行）

- 本次改动前的轻量 baseline 与 Console 结果仍待本人确认，历史通过不作为新结果。
- 三个手工序列：玩家由远到近再离开、Attack 中被击杀、目标引用失效。本人报告做过脑内推演，但尚无完整可复核记录；不把本草稿当作本人独立推演结果。
- 后续实现检查：双阈值与等号、攻击距离/间隔（含首次攻击等待）、攻击期间允许移动、一次攻击只结算一次、旧碰撞伤害停用、死亡打断、玩家死亡后无后续移动/攻击/生成。
- 回归：多种 Prefab 配置读取、生成和目标注入、玩家攻击/受伤/UI、敌人销毁与清波、三波流程、玩家死亡和 R 重开、Unity 引用及 Console。

这份文档是设计证据，不是已运行 FSM、测试通过或个人理解完全掌握的证明。
